# Changelog

## 2026.09.03.1
- **Recently played** — chips under home; stored in this browser only
- **Search** — ranks title/description matches instead of raw order only
- **Images** — `loading="lazy"` + placeholder when a thumbnail fails
- **Report broken game** — link to open a GitHub issue
- **Update toast** — shows once per `version.json` bump
- **Mobile** — larger tap targets, denser game grid, 16px search input
- **Service worker** — caches dashboard shell for brief offline reopen

## Notes
Classic + New homepage both load `JS/nx-ux.js` when linked from HTML.
Settings unification: prefer `Settings/newsettings.html` as the control panel.
